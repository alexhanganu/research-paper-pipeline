# Research Paper Processing Pipeline - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# API Keys (Required)
# ============================================
# Get Anthropic API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Get OpenAI API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Default AI provider to use (anthropic or openai)
DEFAULT_PROVIDER=anthropic

# ============================================
# PubMed Configuration (Optional)
# ============================================
# Your email (required by NCBI for API access)
PUBMED_EMAIL=your.email@example.com

# NCBI API key (optional, increases rate limit from 3 to 10 req/sec)
# Get from: https://www.ncbi.nlm.nih.gov/account/
NCBI_API_KEY=

# ============================================
# Email Notifications (Optional)
# ============================================
# For Gmail, use App Password (not regular password)
# Setup: Google Account → Security → 2-Step Verification → App passwords
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=

# For other email providers:
# Outlook: smtp-mail.outlook.com:587
# Yahoo: smtp.mail.yahoo.com:587
# Custom SMTP: your.smtp.server:port

# ============================================
# Processing Configuration
# ============================================
# Number of parallel workers (1-20)
MAX_WORKERS=5

# Use LangChain for structured extraction (true/false)
USE_LANGCHAIN=false

# Default directories
PAPERS_DIR=project/papers
OUTPUT_DIR=project/outputs

# ============================================
# Cloud Storage (Optional)
# ============================================
# Options: local, aws, azure
CLOUD_PROVIDER=local

# ============================================
# AWS Configuration (if using AWS)
# ============================================
AWS_REGION=us-east-1
AWS_S3_BUCKET=

# For CloudWatch metrics (optional)
ENABLE_CLOUDWATCH=false

# ============================================
# Azure Configuration (if using Azure)
# ============================================
# Get from: Azure Portal → Storage Account → Access keys
AZURE_STORAGE_CONNECTION_STRING=

# Container name for storing papers and results
AZURE_CONTAINER_NAME=research-papers

# For email notifications via Azure Communication Services
AZURE_COMMUNICATION_CONNECTION_STRING=

# ============================================
# Advanced Configuration (Optional)
# ============================================
# Model selection
CLAUDE_MODEL=claude-sonnet-4-20250514
OPENAI_MODEL=gpt-4o

# API parameters
MAX_TOKENS=4000
TEMPERATURE=0.3

# Retry configuration
MAX_RETRIES=3
RETRY_MIN_WAIT=4
RETRY_MAX_WAIT=10

# Quality checks
ENABLE_QUALITY_CHECKS=true
MIN_QUALITY_SCORE=0.5

# Biomarker aggregation
MIN_PAPERS_FOR_HIGH_CONFIDENCE=2

# Cost estimation (per million tokens)
CLAUDE_INPUT_COST=3.0
CLAUDE_OUTPUT_COST=15.0
OPENAI_INPUT_COST=2.5
OPENAI_OUTPUT_COST=10.0

# Logging
LOG_LEVEL=INFO
LOG_FILE=

# ============================================
# Notes
# ============================================
# - At minimum, set one API key (ANTHROPIC_API_KEY or OPENAI_API_KEY)
# - All other settings are optional with sensible defaults
# - For local-only usage, only API keys are needed
# - For cloud deployment, configure the respective cloud provider section
# - For email notifications, configure SMTP settings
# - Lines starting with # are comments and will be ignored